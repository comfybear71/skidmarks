# Copy to MY MOVIES\.env and/or blueprint/planner/.env — never commit real values.
# Restart `npm run dev` after changes.

# Voice (Speak / Gen mp3)
ELEVENLABS_API_KEY=

# Character Lab / plate gen (Grok Imagine)
XAI_API_KEY=

# Optional — force Comfy base URL. Else Studio picks first running RunPod :3000.
# Example: https://YOURPODID-3000.proxy.runpod.net
COMFY_URL=

# RunPod API (pod list / status in Studio header) — keep private
RUNPOD_API_KEY=

# Neon — Vercel uses these when present. Local Studio still reads Desktop folders.
# Leave blank here. Never put real values in git.
DATABASE_URL=
DATABASE_URL_UNPOOLED=
PGHOST=
PGDATABASE=
PGUSER=
PGPASSWORD=
PGPORT=

# Vercel Blob — Vercel uses these when present. Do not auto-upload from local.
BLOB_READ_WRITE_TOKEN=
BLOB_STORE_ID=
BLOB_WEBHOOK_PUBLIC_KEY=
