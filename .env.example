# Copy to MY MOVIES\.env and/or blueprint/planner/.env — never commit real values.
# Restart `npm run dev` after changes.

# Voice (Speak / Gen mp3)
ELEVENLABS_API_KEY=

# Character Lab / plate gen (Grok Imagine stills) + Scratch Grok image-to-video.
# Video: grok-imagine-video-1.5, 1–15s (desk chips 2 / 3 / 5). Same key as stills.
# Invented Grok audio is stripped on the way in so the song can sit under.
XAI_API_KEY=

# Siray — Scratch pad Spicy stills + optional i2v. Create at https://console.siray.ai/keys
# Draw uses Seedream 4.5 ref2i Spicy (~$0.04/image) when other models refuse the still.
# Scratch Generate can still pick Seedance 2.0 i2v Spicy or Wan. Motion only — LTX still
# owns the Saved mp3. Short music-video cuts prefer Grok i2v (XAI_API_KEY). Scratch Draw
# does not silently fall back to XAI.
SIRAY_API_KEY=

# Comfy Cloud — the real animate backend + Comfy MCP (https://cloud.comfy.org/mcp).
# Same key for desk LTX and Cursor `.cursor/mcp.json`. Keys already live in Cursor.
# He is fully subscribed (2026-08-23). Still ask before a generate.
# Leave COMFY_URL blank.
COMFY_CLOUD_API_KEY=

# Self-hosted Comfy only — leave blank when using Comfy Cloud above.
COMFY_URL=

# Neon — Vercel uses these when present. Local Studio still reads Desktop folders.
# Leave blank here. Never put real values in git.
DATABASE_URL=
DATABASE_URL_UNPOOLED=
PGHOST=
PGDATABASE=
PGUSER=
PGPASSWORD=
PGPORT=

# Vercel Blob — Vercel uses these when present. Do not auto-upload from local.
BLOB_READ_WRITE_TOKEN=
BLOB_STORE_ID=
BLOB_WEBHOOK_PUBLIC_KEY=

# Who can use M / Scratch / Crash Lab. Homepage stays public either way.
# Leave blank = no login (this PC / cloud VM).
# On Vercel Production set two emails, comma-separated:
# STUDIO_USERS=you@example.com:password,mum@example.com:otherpassword
STUDIO_USERS=
# Optional. Cookie HMAC. Defaults to STUDIO_USERS if blank. Set a random string on Vercel.
STUDIO_SESSION_SECRET=
